import { MailLinkUnstyled } from '../../_components/mail-link';
import { MdxInnerWrapper } from '../../_components/mdx-layout';

export const metadata = {
    title: 'Privacy policy - HKViz',
    alternates: {
        canonical: '/privacy-policy',
    },
};

# The Privacy Policy is not accessible in the archived version of HKViz

Visit [https://hkviz.org/privacy-policy](https://hkviz.org/privacy-policy) to read the current Privacy Policy of HKViz.

export const runtime = 'edge';

export default function MDXPage({ children }) {
    return <MdxInnerWrapper>{children}</MdxInnerWrapper>;
}
