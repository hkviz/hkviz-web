{
    "name": "hkviz-web",
    "version": "0.0.1",
    "description": "",
    "scripts": {
        "dev": "pnpm --parallel --reporter append-only --color=always --reporter=append-only --filter=!app2 run dev",
        "dev:solid": "pnpm --parallel --reporter append-only --color=always --reporter=append-only --filter=!app run dev",
        "build": "pnpm --recursive --reporter append-only --color=always --reporter=append-only --filter=!app2 run build",
        "build:solid": "pnpm --recursive --reporter append-only --color=always --reporter=append-only --filter=!app run build",
        "lint": "pnpm --recursive --reporter append-only --color=always run lint",
        "lint:fix": "pnpm --recursive --reporter append-only --color=always run lint:fix "
    },
    "keywords": [],
    "author": "",
    "license": "ISC",
    "packageManager": "pnpm@8.15.8",
    "devDependencies": {
        "prettier": "^3.3.3",
        "tsup": "^8.3.5",
        "tsup-preset-solid": "^2.2.0",
        "turbo": "^1.13.4"
    },
    "dependencies": {
        "esbuild-plugin-solid": "^0.6.0",
        "next": "^14.0.4"
    }
}
